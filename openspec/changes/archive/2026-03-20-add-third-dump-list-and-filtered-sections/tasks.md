## 1. 数据分组与过滤实现

- [x] 1.1 在 BTM ViewModel 中保留完整 dump 条目集合，作为三组列表的统一数据源
- [x] 1.2 实现“登录时打开”分组过滤逻辑：仅保留分类已确认为登录项且开关开启的条目
- [x] 1.3 实现“允许在后台”分组过滤逻辑：仅保留分类已确认为后台项且开关开启的条目
- [x] 1.4 实现“完整 Dump 数据”分组数据映射：展示全部已解析条目且不应用上述过滤

## 2. 界面分组与状态文案更新

- [x] 2.1 更新侧栏分组定义与顺序，新增“完整 Dump 数据”入口并接入对应列表数据
- [x] 2.2 更新主列表空状态文案，明确前两组仅展示“已确认且已开启”条目
- [x] 2.3 确认三组列表中的行级开关与详情跳转交互保持现有行为且互不干扰

## 3. 回归验证与边界场景

- [x] 3.1 验证混合数据场景（已开启/未开启、已确认/未确认）下三组列表条目归属正确
- [x] 3.2 验证前两组筛选后为空时，完整 Dump 分组仍可访问且展示完整数据
- [x] 3.3 验证分类不完整提示仍会出现，且不确定条目仅在完整 Dump 分组中可见
