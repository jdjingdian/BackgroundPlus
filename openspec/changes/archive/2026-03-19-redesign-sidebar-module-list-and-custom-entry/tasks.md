## 1. 信息架构重构

- [x] 1.1 重构主窗口容器：侧边栏仅保留功能导航项（如后台模块、设置、关于）
- [x] 1.2 将后台模块列表从侧边栏迁移到主区域并保持现有数据加载链路
- [x] 1.3 调整页面状态模型，拆分“当前功能路由”与“当前条目上下文”

## 2. 后台模块列表行改造

- [x] 2.1 实现系统风格列表行组件，包含 icon、标题、副标题、开关与 `→` 按钮布局
- [x] 2.2 接入系统原生图标读取逻辑并实现失败回退占位图标
- [x] 2.3 保证开关与 `→` 的点击热区独立，避免互相误触

## 3. 我的定制功能导航接入

- [x] 3.1 在每个列表行的开关右侧接入 `→` 跳转动作并传递条目标识参数
- [x] 3.2 复用 `BackgroundItemDetailView` 作为“我的定制功能”展示页并完成路由绑定
- [x] 3.3 实现无效 app 标识时的降级行为（禁用跳转或提示无法打开详情）
- [x] 3.4 实现从详情返回列表时保留滚动位置与条目上下文

## 4. 回归与可用性验证

- [x] 4.1 验证 helper 已安装/未安装两种状态下列表展示与提示行为
- [x] 4.2 验证解析不完整场景下仍可展示可用条目且提示准确
- [x] 4.3 验证图标读取失败、长文本截断、多条目滚动等 UI 边界场景
- [x] 4.4 更新本地化文案键值，确保新增按钮与提示支持中英文
