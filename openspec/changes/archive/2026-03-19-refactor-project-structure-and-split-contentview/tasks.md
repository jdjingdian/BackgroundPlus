## 1. 结构梳理与迁移准备

- [x] 1.1 盘点 `ContentView` 当前职责，标记壳层布局、状态编排、可抽取展示区块。
- [x] 1.2 设计目标目录映射（`Screens`、`Components`、`Sheets/Panels`）并明确各文件迁移落点。
- [x] 1.3 确认 `Shared` 与 `StaticRouter/View/Components` 的边界，记录可复用组件归属规则。

## 2. ContentView 拆分实施

- [x] 2.1 提取纯展示子视图（无副作用）并替换 `ContentView` 中对应内联 UI。
- [x] 2.2 提取页面容器视图，迁移业务区域状态编排与事件转发逻辑。
- [x] 2.3 将 `ContentView` 收敛为壳层入口，仅保留顶层布局和依赖装配。

## 3. 现有能力对齐迁移

- [x] 3.1 调整 helper 安装引导相关 UI 到独立设置容器模块，保持状态展示和触发流程一致。
- [x] 3.2 调整 BTM 列表相关 UI 到独立列表容器模块，保持列表字段与数据来源行为一致。
- [x] 3.3 修复迁移造成的引用路径、依赖注入或预览编译问题。

## 4. 验证与收尾

- [x] 4.1 执行构建验证，确保重构后工程可编译通过。
- [x] 4.2 按关键路径进行回归（设置入口、helper 安装状态、BTM 列表展示、路由管理流程）。
- [x] 4.3 清理无用旧代码并更新目录约定说明，确保后续增量开发遵循新结构。
