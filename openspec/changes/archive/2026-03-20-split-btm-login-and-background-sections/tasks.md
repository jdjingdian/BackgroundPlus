## 1. 分类模型与数据投影

- [x] 1.1 在 BTM 解析结果模型中增加分组类别字段（登录时打开/允许在后台/未知）
- [x] 1.2 实现统一分类映射层，将 dump 条目投影为两个列表数据集
- [x] 1.3 为分类规则补充单元测试，覆盖 app/developer/legacy daemon 与缺字段场景

## 2. 侧栏与导航结构

- [x] 2.1 将原“后台模块”单入口调整为“登录时打开”“允许在后台”两个侧栏入口
- [x] 2.2 更新路由状态与默认选中逻辑，确保返回导航保留当前分组上下文
- [x] 2.3 更新本地化文案键与显示文案，避免硬编码分组名称

## 3. 列表渲染与交互保持

- [x] 3.1 让列表容器按当前分组加载对应数据集并展示独立空状态
- [x] 3.2 保持条目行的图标、开关、`→` 详情入口与可访问性语义不变
- [x] 3.3 实现“分类不完整”提示，不阻断可确定分类条目的展示

## 4. 回归与验收

- [x] 4.1 回归 helper 未安装状态在两个分组下的提示与引导行为
- [x] 4.2 回归详情页进入/返回流程，确认列表滚动与选中上下文保持
- [x] 4.3 增补 UI/集成测试，验证双分组切换、空状态与开关交互互不干扰
