## 1. Helper 兼容性握手接口

- [x] 1.1 在 helper 中实现稳定的能力读取接口，返回 helper 版本与接口版本信息
- [x] 1.2 为能力读取接口补充兼容性约束（禁止移除/破坏返回结构）并更新相关调用封装
- [x] 1.3 为能力读取接口添加单元或集成验证，覆盖正常返回与异常返回

## 2. App 侧版本校验与调用阻断

- [x] 2.1 在 app 启动后首次使用 helper 前增加握手流程并缓存校验结果
- [x] 2.2 实现 app 版本与 helper 版本比对逻辑，定义不匹配状态为“需重装 helper”
- [x] 2.3 在高风险 helper 调用入口增加前置守卫：校验未通过时统一阻断调用
- [x] 2.4 在能力读取失败场景复用不兼容处置路径，避免继续执行特权调用

## 3. 用户提示与重装引导

- [x] 3.1 在设置或相关入口增加高优先级强提示 UI，明确说明版本不匹配风险
- [x] 3.2 在强提示中提供“重装 helper”操作入口，并在完成后触发重新校验
- [x] 3.3 补充错误文案与状态展示（未安装/不匹配/读取失败/已恢复）的一致性检查

## 4. 回归与发布保障

- [x] 4.1 增加场景测试：旧 helper + 新 app、能力读取失败、重装后恢复
- [x] 4.2 增加日志与诊断字段，确保可定位版本不匹配和握手失败原因
- [x] 4.3 完成回归验证并记录回滚方案（保持不兼容调用默认阻断）
