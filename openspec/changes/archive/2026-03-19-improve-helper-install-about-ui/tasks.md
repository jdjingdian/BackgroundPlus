## 1. 布局与样式基础

- [x] 1.1 梳理当前设置窗口中“通用/关于”页面结构，确定可复用的容器与分区边界。
- [x] 1.2 实现统一设置页容器（顶部标签区、主内容区、底部操作区）并接入共享间距与分割线样式。
- [x] 1.3 提炼状态文本、主次按钮与危险按钮的统一样式规则，确保“关闭”与“卸载”视觉可区分。

## 2. 通用页（Helper 安装）改造

- [x] 2.1 重构 Helper 状态信息块，将状态图标、状态文案、说明文本与操作按钮整合到同一区域。
- [x] 2.2 按 Helper 安装状态切换按钮文案与可用性，覆盖“已安装/未安装”两种场景。
- [x] 2.3 新增可见关闭按钮并绑定退出行为，保证与 `Esc` 快捷退出行为一致。
- [x] 2.4 调整通用页整体间距与对齐，使信息层级与参考视觉保持一致。

## 3. 关于页改造

- [x] 3.1 重构关于页头部信息区，展示应用图标、名称、版本和简要说明。
- [x] 3.2 增加许可证与版权信息区，并与主内容区保持统一分割与排版规范。
- [x] 3.3 保留并规范“项目主页”入口按钮样式与交互位置，确保与页面视觉语言一致。

## 4. 验证与回归

- [x] 4.1 手动验证 Helper 页关闭按钮、`Esc` 退出、安装/卸载操作在不同状态下行为正确。
- [x] 4.2 手动验证“通用/关于”页面在常见窗口尺寸下的布局稳定性与可读性。
- [x] 4.3 更新变更说明或截图（如仓库流程需要），记录最终 UI 行为与已覆盖场景。
